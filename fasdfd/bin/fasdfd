
#!/usr/bin/env ruby

require 'fasdfd'

data = Fasdfd::Magic.new.call

data.instance_variables.each do |v|
  method = v.gsub("@", "")
  method_name = method.gsub("_", " ")
  puts method_name + ": " + data.send(method)
end

